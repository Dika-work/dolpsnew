import 'package:flutter/material.dart';

class DataUser extends StatelessWidget {
  const DataUser({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('ini data user'),
      ),
    );
  }
}
